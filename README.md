# check-system-screen-time

##To check the screen time in ubuntu, we can use the following command:
    uptime
here are its options:
    -p , --pretty 
    -h, --help
    -s, --since
    -V, --version
